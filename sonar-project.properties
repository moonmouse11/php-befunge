# SonarQube / SonarCloud project configuration.
# Secrets (SONAR_TOKEN, SONAR_HOST_URL) live in the GitHub repository settings.

sonar.projectKey=pots_php-befunge
sonar.organization=pots

sonar.sources=src
sonar.php.file.suffixes=php

# Clover coverage report produced by PHPUnit in CI.
sonar.php.coverage.reportPaths=clover.xml

# Fail the build when the Quality Gate fails.
sonar.qualitygate.wait=true

sonar.sourceEncoding=UTF-8
